<div data-controller="ajax">
  <%= render partial: "calendar", locals: { calendar: @calendar } %>

  <div class="habits" data-ajax-target="habits">
    <%= render partial: "habit", collection: @habits, locals: @habits_data %>
  </div>

  <div class="actions">
    <%= link_to reset_habits_path, data: { turbo_method: :delete } do %>
      <%= icon :rotate %>
      <div>reset day</div>
    <% end %>
    <%= link_to new_habit_path do %>
      <%= icon :square_plus %>
      <div>add habit</div>
    <% end %>
    <%= link_to timer_path do %>
      <%= icon :timer %>
      <div>timer</div>
    <% end %>
  </div>
</div>
