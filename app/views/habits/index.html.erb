<h3><%= @date %></h3>

<%= render partial: "calendar", locals: @calendar_data %>

<div class="mb">
  <% @habits.each do |habit| %>
    <div>
      <% if habit.done_today? %>
        <div class="habit habit-done"><%= habit.name %></div>
      <% else %>
        <%= link_to habit.name, done_habit_path(habit), data: { turbo_method: :post }, class: "habit" %>
      <% end %>
    </div>
  <% end %>
</div>

<%= link_to reset_habits_path, data: { turbo_method: :delete }, class: "reset-icon" do %>
    <%= render "shared/icons/rotate" %>
<% end %>
